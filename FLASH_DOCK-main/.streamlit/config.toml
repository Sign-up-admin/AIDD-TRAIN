[theme]
primaryColor = "#FF4B4B"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#262730"
font = "sans serif"

[server]
# 启用 CORS 以兼容 XSRF 保护
# XSRF 保护需要 CORS 来发送 cookie，因此必须启用 CORS
enableCORS = true
enableXsrfProtection = true
